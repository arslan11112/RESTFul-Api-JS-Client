# MVC Movie Catalogue web application

 MVC (Model, Controller, View) web application that uses multiple Java servlets and JSP views to allow the user to have full CRUD functionality on the film DB.  
